#!/bin/bash

# Define options with glyphs
OPTIONS="  Lock\n  Reboot\n  Shutdown"

# Show menu using walker
SELECTION=$(echo -e "$OPTIONS" | walker --dmenu --width 500 --minheight 1 -p " Power Options")

# Map selection to actions
case "$SELECTION" in
"  Lock")
  lock-session
  ;;
"  Reboot")
  systemctl reboot
  ;;
"  Shutdown")
  systemctl poweroff
  ;;
*)
  exit 0
  ;;
esac
